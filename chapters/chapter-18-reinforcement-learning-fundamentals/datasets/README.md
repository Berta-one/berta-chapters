# Chapter 18 Datasets

RL generates its own data through interaction, so there is no static dataset. The gridworld layout is in `gridworld_layout.txt`.

- `.` free cell, `#` wall, `S` start, `G` goal.
